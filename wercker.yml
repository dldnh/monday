# from mark's first wercker how-to session

box:
  id: store/oracle/serverjre
  username: $DOCKER_USERNAME
  password: $DOCKER_PASSWORD
  tag: 8

# this pipeline does not really do anything
build:
  steps:
    - script:
      name: Build
      code: |
        echo "Hello, world!"

dev:
  steps:
    - script:
      name: Build
      code: |
        echo "Hello, world!"
    - internal/shell

hi-again:
  steps:
    - script:
      name: Hi
      code: |
        echo "Hi again!"

